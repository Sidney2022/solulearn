{
    "builds": [{
        "src": "solulearn/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "solulearn/wsgi.py"
        }
    ]
}

